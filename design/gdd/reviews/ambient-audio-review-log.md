# Ambient Audio — Review Log

## Review — 2026-08-05 — Verdict: NEEDS REVISION → resolved same session → Approved
Scope signal: M (4 soft dependencies, none requiring reciprocal companion edits; 3 formula groups expanded to 4 after this round's redesign; no new ADR required)
Specialists: game-designer, systems-designer, qa-lead, performance-analyst, gameplay-programmer, godot-specialist, ux-designer, creative-director (full-mode `/design-review`, first fresh-session round for this document — the last of the 11 MVP GDDs to receive one)
Blocking items: 12 | Recommended: 3
Summary: Unusually strong specialist convergence this round — three specialists (performance-analyst, gameplay-programmer, godot-specialist) independently flagged an unaddressed browser autoplay-policy gap (fixed with a new `PENDING_GESTURE` state, fade-in deferred to the player's first input), and two (systems-designer, qa-lead) independently confirmed the same arithmetic error in AC16, tracing it to a root cause: `watering_layer_db` returned an absolute level while `discovery_boost_db` returned a bare delta, two incompatible units combined as one. Both reactive-layer formulas were redesigned as pure deltas, combined through one new `final_volume_db` function under two explicit ceilings (`COMBINED_BOOST_MAX_DB`, `HEADROOM_CEILING_DB`) — closing the arithmetic error, the missing combined-boost ceiling, and an undocumented clipping/headroom risk in one change. `creative-director` also accepted a `game-designer` finding that the uniform per-category discovery-cue treatment let concurrent cue *count* outrank cue *rarity* (three mundane Growth ticks could outweigh one Detail Event "payoff") — fixed by adding a `CATEGORY_WEIGHT` term and demoting `N_active` to a secondary term, while keeping the texture itself uniform (preserving the original anti-informational intent). A stale `N_active` range (1–3, citing `discovery-surfacing.md`'s pre-correction cap) was widened to 1–5. `ux-designer`'s finding that this system's mute control — the game's sole exception to a locked "zero UI chrome" anchor — was left entirely unconstrained was upheld with the same standard applied to Diorama Rendering's vignette ruling this session: the exception to a locked rule must be scoped tighter, not looser. Two inaccurate Godot-API claims (Ogg "streamed" import mode; an implied EQ/filter effect never backed by any formula) were corrected after `godot-specialist` verification against this project's own pinned engine-reference docs. Loop length was raised from 90-150s to 4-6 minutes after `game-designer` showed the original length would repeat 24-80 times across a typical long session, undercutting its own "macro photograph you could stare at for an hour" claim.
Prior verdict resolved: First review (no prior review-log entry existed)
