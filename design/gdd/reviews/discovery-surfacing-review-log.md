# Discovery Surfacing — Review Log

## Review — 2026-08-05 — Verdict: NEEDS REVISION → resolved same session → Approved
Scope signal: M (2 hard upstream dependencies with clean reciprocation, 1 downstream companion-edit dependency, 3 formulas, no new ADR required)
Specialists: game-designer, systems-designer, qa-lead, performance-analyst, gameplay-programmer, godot-specialist, creative-director (full-mode `/design-review`, first fresh-session round for this document)
Blocking items: 4 | Recommended: 7
Summary: All 4 blockers were text-level. (1) `systems-designer` found the doc's "5 concurrent cues" and "66.0s duration ceiling" claims were presented as one shared "legal extreme" but actually require two different, mutually exclusive knob combinations — corrected in 3 places. (2) `performance-analyst` found this concurrency correction had never propagated to `diorama-rendering.md:426`, which still cited the stale "2-3" figure and asserted an unverified "stays within budget" confidence claim — fixed as a companion edit to that document. (3) `creative-director`, ruling on a `game-designer` finding, struck "the queue's designed payoff"/"climax"/"most lingering" framing from Arrival's cue description and Core Rule 8's rationale — the framing directly contradicted Core Rules 6 and 9 (no acknowledgment tracking, gameplay never waits on the queue), which guarantee a player may leave before this cue, which always activates last, ever fires. Tier ordering itself was kept (stands on anti-overshadowing rationale alone). (4) `creative-director` struck Core Rule 6's citation of Pillar 2 ("nothing is ever late") as backwards — a cue that vanishes forever if unseen is itself late in the sense Pillar 2 forbids; re-cited to the Anti-Pillar against demanding attention instead. A 5th user-adjudicated item (`gameplay-programmer` finding: no named config location for `pacing_delay`/`cue_fade_duration`) was resolved by adding a data-driven note, matching project convention rather than pinning a file format. A 6th (`gameplay-programmer` finding: Core Rule 4a's BLOCKED gate has no fallback) was resolved by explicitly matching sibling-doc precedent (`time-drift.md`, `input-abstraction.md`) — no fallback added, user's explicit choice. Recommended items (AC coverage gaps for mid-fade backgrounding and Arrival/Departure tie-breaks, queue architecture/ownership notes, verification-prototype sequencing advice) were left open for a future pass, not blocking.
Prior verdict resolved: First review (no prior review-log entry existed; the document's own header already recorded an informal in-session "round 1" prior to this logged round)
