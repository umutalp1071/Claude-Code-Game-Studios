# Diorama Rendering — Review Log

## Review — 2026-08-05 — Verdict: MAJOR REVISION NEEDED → resolved same session → Approved
Scope signal: L (6 hard upstream dependencies, all reciprocated; 5 formulas after this round's addition; no new ADR strictly required yet, but Open Question 1's verification prototype is now blocking and will likely need one once resolved)
Specialists: game-designer, systems-designer, qa-lead, performance-analyst, gameplay-programmer, godot-specialist, technical-artist, creative-director (full-mode `/design-review`, first fresh-session round for this document)
Blocking items: 8 | Recommended: 5
Summary: All four formulas verified correct at every boundary by `systems-designer`; the defects were conceptual, not arithmetic. Three findings converged on the same root cause — this document's own "would it still make physical sense without the glow?" corrective test was violated in three separate places: the STALLED cue's flat grey-blue tint (replaced with an etiolation-based pale yellow-green, `game-designer`/`technical-artist`), a vignette sprite falsely claimed as equivalent to the locked Visual Identity Anchor's "shallow depth of field" (replaced with baked far-plane asset authoring, `technical-artist`, disagreement with `godot-specialist`'s "cheap and safe" framing resolved in `technical-artist`'s favor — engineering feasibility doesn't establish fidelity to a locked anchor), and a "sun" accent light whose *direction* tracked day/night against every sprite's fixed baked shadow (direction now fixed, only color/intensity drift, `technical-artist`). A fourth structural finding (`game-designer`): Core Rule 2's zero-interpolation growth-stage rendering was justified as "dead code, player never present to see it," but in a cozy check-in game session boundaries are witnessed constantly — `creative-director` accepted the reframe and added Core Rule 2a (Catch-up Growth Reveal), which eases the growth-pattern *scale transform* only (never the sprite/texture, avoiding cross-fade cost) using `{from, to}` values Discovery Surfacing already computes, and reworded Core Rule 9 from a motion prohibition to a data-integrity guarantee. A fifth (`technical-artist`, unflagged elsewhere): non-uniform growth scaling was stretching the baked-in shadow shape at extreme divergence — capped at 0.30 per-axis, climb retuned. A sixth (`performance-analyst`/`qa-lead`): this document's own Edge Cases said a draw-call budget claim was "not yet verified," while its Visual/Audio Requirements asserted the same claim as settled fact with zero supporting arithmetic — the confident claim was struck and a real AC added to enforce the named `/smoke-check` gate. A user decision resolved the Discovery-cue-rendering-mechanism gap (`gameplay-programmer`): all 4 cue categories now explicitly use real `Light2D` instances per the corrective test's own logic, pushing the true worst-case concurrent-light count to 8 (1-3 ambient + up to 5 cue-driven) — which in turn made Open Question 1 (unverified Light2D/Compatibility-renderer behavior) load-bearing enough to escalate from advisory to blocking. Recommended items (a flawed "snap-back distance can never be 0" edge-case claim, an `entities.yaml` registry gap, a missing local defensive clamp, an unstated Tween kill-and-restart implementation pattern, and 4 missing ACs) were all resolved in the same pass.
Prior verdict resolved: First review (no prior review-log entry existed)
